import 'package:flutter/material.dart';

class TrackingDailySection extends StatelessWidget {
  const TrackingDailySection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(),
    );
  }
}
