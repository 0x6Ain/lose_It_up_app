name: Run Tests
on: [push, workflow_dispatch]
jobs:
  drive:
    runs-on: ubuntu-latest
    # https://stackoverflow.com/a/63122434/436422
    defaults:
      run:
        working-directory: lose_it_up_app
    steps:
      - uses: actions/checkout@v1
      - uses: subosito/flutter-action@v1
      - run: flutter test
